//! Binary package marker; the daemon library is provided by `subc-daemon`.
//!
//! This empty library permits a self dev-dependency to enable the dedicated
//! CLI fixture targets during integration tests without enabling them by default.

#![forbid(unsafe_code)]
